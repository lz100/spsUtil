% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operators.R
\name{inc}
\alias{inc}
\alias{mult}
\alias{dev}
\title{In-place operations}
\usage{
inc(e1, e2 = 1)

mult(e1, e2 = 2)

dev(e1, e2 = 2)
}
\arguments{
\item{e1}{object, most likely a numeric object}

\item{e2}{the operation value, the value to add, subtract, multiply, divide of.}
}
\value{
No return, directly assign the value back to \code{e1}
}
\description{
In-place operations like \verb{i += 1}, \verb{i -= 1} is not support in
R. These functions implement these operations in R.
}
\details{
\code{inc(i)} is the same as \code{i <- i + 1}.
\code{inc(i, -1)} is the same as \code{i <- i - 1}.
\code{mult(i)} is the same as \code{i <- i * 2}.
\code{dev(i)} is the same as \code{i <- i / 2}.
}
\examples{
i <- 0
inc(i) # add 1
i
inc(i) # add 1
i
inc(i, -1) # minus 1
i
inc(i, -1) # minus 1
i
x <- 1
mult(x) # times 2
x
mult(x) # times 2
x
dev(x) # divide 2
x
dev(x) # divide 2
x
}
\seealso{
If you want reactiveVal or reactiveValues version of these operators,
check \link{spsComps}
}
